const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);$('#year').textContent=new Date().getFullYear();const menu=$('.menu-btn'),nav=$('.nav-links');menu?.addEventListener('click',()=>nav.classList.toggle('open'));const toggle=$('#themeToggle');toggle?.addEventListener('click',()=>{document.body.classList.toggle('dark-ui');toggle.textContent=document.body.classList.contains('dark-ui')?'☀':'☾'});const cards=[...$$('.card')],filters=[...$$('.filter')],search=$('#search'),empty=$('#empty');let active='all';function render(){const q=(search.value||'').toLowerCase();let shown=0;cards.forEach(c=>{const ok=(active==='all'||c.dataset.cat.includes(active))&&(c.dataset.title+' '+c.innerText).toLowerCase().includes(q);c.style.display=ok?'block':'none';if(ok)shown++});empty.hidden=shown!==0}filters.forEach(f=>f.addEventListener('click',()=>{filters.forEach(x=>x.classList.remove('active'));f.classList.add('active');active=f.dataset.filter;render()}));search.addEventListener('input',render);